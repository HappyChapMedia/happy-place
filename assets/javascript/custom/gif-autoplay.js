window.addEventListener("load", videoAutoPlayTest, false);

function playVideo() {
	var videoTag = document.querySelector('video');
  videoTag.play();
}

function videoAutoPlayTest() {
  var video = document.createElement('video');
  
}